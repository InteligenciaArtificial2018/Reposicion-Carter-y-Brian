package tech.inversa.todolistapp.data

import android.arch.persistence.room.ColumnInfo
import android.arch.persistence.room.Entity
import android.arch.persistence.room.ForeignKey
import android.arch.persistence.room.PrimaryKey

//@Entity(tableName = ,
  //      foreignKeys = arrayOf(ForeignKey(
        //    entity = tabla::class nombre de la tabla que queres relacionar con esta,
    ////        parentColumns = arrayOf("campoEnotraTabla") nombre del campo que es llave primaria en la tabla que queres relacionar,
          //  childColumns = arrayOf("campoEnEstaTabla") nombre del campo que es llave foranea en esta tabla
        //)))
class ejemplo(
    /*@ColumnInfo(name = "idCategoriaBaja")
    var asunto: String = "",
    @ColumnInfo(name = "idCategoriaMedia")
    var detalle: String = "",
    @ColumnInfo(name = "idCategoriaAlta")
    var  monto: String = ""){
    @PrimaryKey(autoGenerate = true)
    var id: Int = 0*/
)